import { supabase } from './supabase.js';
import { STAFF_AUTH_FUNCTION, TARGET_YEAR } from './config.js';
let verificationToken='', loginEmail='', needsSetup=false;
const verifyForm=document.getElementById('verifyForm'), passwordPanel=document.getElementById('passwordPanel');
const errorBox=document.getElementById('error'), passwordError=document.getElementById('passwordError');
const show=(el,msg)=>{el.textContent=msg;el.style.display='block'}; const hide=el=>el.style.display='none';
verifyForm.addEventListener('submit',async(e)=>{e.preventDefault();hide(errorBox);const staff_id=document.getElementById('staffId').value.trim();const birth_date=document.getElementById('birthDate').value.trim();if(!/^\d{4}$/.test(staff_id)||!/^\d{8}$/.test(birth_date)){return show(errorBox,'スタッフIDは4桁、生年月日は8桁の数字で入力してください。')}
 const {data,error}=await supabase.functions.invoke(STAFF_AUTH_FUNCTION,{body:{action:'verify',staff_id,birth_date,year:TARGET_YEAR}});if(error||!data?.ok)return show(errorBox,data?.message||'本人確認に失敗しました。');verificationToken=data.verification_token;loginEmail=data.login_email||'';needsSetup=!!data.needs_password_setup;verifyForm.classList.add('hidden');passwordPanel.classList.remove('hidden');document.getElementById('passwordTitle').textContent=needsSetup?'初回パスワード設定':'パスワード入力';document.getElementById('passwordLabel').textContent=needsSetup?'新しいパスワード':'設定済みパスワード';document.getElementById('passwordSubmit').textContent=needsSetup?'設定してログイン':'ログイン';document.getElementById('confirmField').classList.toggle('hidden',!needsSetup);document.getElementById('password').autocomplete=needsSetup?'new-password':'current-password';});
document.getElementById('passwordForm').addEventListener('submit',async(e)=>{e.preventDefault();hide(passwordError);const password=document.getElementById('password').value;if(password.length<8)return show(passwordError,'パスワードは8文字以上で設定してください。');if(needsSetup){const confirm=document.getElementById('passwordConfirm').value;if(password!==confirm)return show(passwordError,'確認用パスワードが一致しません。');const {data,error}=await supabase.functions.invoke(STAFF_AUTH_FUNCTION,{body:{action:'set-password',verification_token:verificationToken,password}});if(error||!data?.ok)return show(passwordError,data?.message||'パスワード設定に失敗しました。');loginEmail=data.login_email;}
 const {error:signInError}=await supabase.auth.signInWithPassword({email:loginEmail,password});if(signInError)return show(passwordError,'パスワードが正しくありません。');location.href='./index.html';});
